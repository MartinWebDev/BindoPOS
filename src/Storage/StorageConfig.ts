export const LocalStorageConfig = {
    // Keys:
    ScannedBarcodes: "ScannedBarcodes" as string
};

export const SQLiteStorageConfig = {
    DatabaseName: "BindoPos" as string,
    DatabaseLocation: "~/database/BindoPos.db" as string
}
